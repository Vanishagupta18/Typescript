function greet(person: string): string {
    return `Hello, ${person}, welcome to our application!`;
}

const username: string = "Alice";
console.log(greet(username));

// The above code defines a simple function to greet a user by name and demonstrates type annotations in TypeScript.